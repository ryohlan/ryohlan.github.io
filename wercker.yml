box: node
build:
  steps:
    - npm-install
    - script:
        name: Deploy
        code: |
          git remote rm origin
          git remote add origin https://ryohlan:$GH_TOKEN@github.com/ryohlan/ryohlan.github.io.git
          npm run build
          git add -A
          git co -m "deploy from Wercker"
          git push origin source
          npm run deploy